import { TestBed } from '@angular/core/testing';

describe('Example Unit Test', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      // Add your testing module configuration here
    });
  });

  it('should create', () => {
    expect(true).toBe(true);
  });

  it('should perform basic arithmetic', () => {
    expect(1 + 1).toBe(2);
  });
});
